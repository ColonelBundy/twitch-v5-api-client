{
  "name": "twitch-v5-api-client",
  "version": "0.0.1",
  "description": "Client library to query twitch's v5 api",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "cross-env DEBUG=twitch concurrently -kr \"tsc -w -p .\" \"nodemon dist/app.js --watch dist/ --delay 1000ms\"",
    "dev": "cross-env DEBUG=twitch nodemon dist/app.js"
  },
  "author": "makkesk8",
  "license": "MIT",
  "dependencies": {
    "request": "^2.79.0"
  },
  "devDependencies": {
    "typescript": "^2.1.6",
    "cross-env": "^3.1.3",
    "nodemon": "^1.11.0"
  },
  "keywords": [
    "twitch",
    "api",
    "client",
    "v5",
    "typescript",
    "es6"
  ]
}
